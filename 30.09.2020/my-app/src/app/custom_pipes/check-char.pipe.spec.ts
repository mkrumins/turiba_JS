import { CheckCharPipe } from './check-char.pipe';

describe('CheckCharPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckCharPipe();
    expect(pipe).toBeTruthy();
  });
});
